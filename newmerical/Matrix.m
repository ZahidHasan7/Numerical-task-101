A = input('Enter matrix A= ');
B = input('Bnter matrix B= ');
c = columns(A);
r = rows(B);
fprintf('max value of a matrix= %d\n',max(x) );
if r == c
  fprintf('product of x and y: \n');
  disp(A*B);
else
  fprintf('Matrix multiplication is not possible');
  end

